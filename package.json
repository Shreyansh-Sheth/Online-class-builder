{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev  --no-sort",
    "build:frontend": "lerna run build",
    "run:frontend": "lerna run start:frontend",
    "run:backend": "lerna run start:backend"
  },
  "devDependencies": {
    "lerna": "^5.5.0"
  }
}
